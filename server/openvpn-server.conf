# ============================================
# OpenVPN Server - تونل برای دسترسی به اینترنت بین‌المللی
# روی سرور (کامپیوتر شما با IP استاتیک)
# ============================================

port 1194
proto udp
dev tun

# مسیر فایل‌های کلید و گواهی (بعد از ساخت با easy-rsa پر کنید)
ca "/workspaces/vpn/server/ca.crt"
cert "/workspaces/vpn/server/server.crt"
key "/workspaces/vpn/server/server.key"
dh "/workspaces/vpn/server/dh.pem"

# شبکه مجازی برای کلاینت‌ها (آدرس‌های داخل تونل)
server 10.8.0.0 255.255.255.0

# نگه داشتن مسیر برای اتصال مجدد کلاینت‌ها
ifconfig-pool-persist "/workspaces/vpn/server/ipp.txt"

# مسیر اسکریپت بررسی یوزرنیم/پسورد (در صورت نیاز)
script-security 3
# اگر علاوه بر گواهی، یوزرنیم/پسورد هم می‌خواهید این خط را فعال کنید
auth-user-pass-verify "/workspaces/vpn/server/auth.py" via-env
username-as-common-name

# گواهی کلاینت را الزامی کن
remote-cert-tls client

push "redirect-gateway def1 bypass-dhcp"
push "dhcp-option DNS 8.8.8.8"
push "dhcp-option DNS 8.8.4.4"

keepalive 10 120
cipher AES-256-GCM
auth SHA256
max-clients 20

persist-key
persist-tun
status "/workspaces/vpn/server/openvpn-status.log"
log "/workspaces/vpn/server/openvpn.log"
verb 3
explicit-exit-notify 1
